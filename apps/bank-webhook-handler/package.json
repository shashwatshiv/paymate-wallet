{
  "name": "bank-webhook-handler",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "pnpm exec esbuild ./src/index.ts --bundle --platform=node --outfile=dist/index.js",
    "start": "node ./dist/index.js",
    "dev": "pnpm run build && pnpm run start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.14.0",
  "dependencies": {
    "@types/express": "^5.0.3",
    "@repo/db": "workspace:*",
    "esbuild": "^0.25.9",
    "@repo/typescript-config": "workspace:*",
    "express": "^5.1.0"
  }
}
